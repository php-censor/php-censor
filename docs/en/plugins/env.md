Plugin Env
----------

Sets environment variables on the PHP Censor server for the build.

Configuration
=============

### Examples:

```yml
setup:
    env:
        APPLICATION_ENV: "development"
```
